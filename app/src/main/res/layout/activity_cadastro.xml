<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".view.CadastroActivity"
    android:background="@drawable/cityi">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cadastro de CLientes"
        android:id="@+id/projeto"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="22dp"
        android:textSize="25dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Novo Cliente"
        android:id="@+id/btnAdicionar"
        android:layout_below="@+id/projeto"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="22dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total De Registros"
        android:id="@+id/txtContadorContatos"
        android:layout_below="@+id/btnAdicionar"
        android:layout_centerHorizontal="true"
        android:textSize="25dp"
        android:layout_marginTop="38dp"
      />
    <ScrollView
        android:id="@+id/scrollViewContatos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:background="#00BFFF"
        android:layout_below="@+id/txtContadorContatos"
        android:layout_centerHorizontal="true">
        <LinearLayout
            android:id="@+id/objetosContatos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
        </LinearLayout>



    </ScrollView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voltar"
        android:id="@+id/btnVoltar"
        android:layout_below="@+id/btnAdicionar"
        android:layout_alignRight="@+id/btnAdicionar"
        android:layout_alignEnd="@+id/btnAdicionar"
        android:layout_alignLeft="@+id/btnAdicionar"
        android:layout_alignStart="@+id/btnAdicionar" />

</RelativeLayout>
